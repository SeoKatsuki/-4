# Задание 1

# Подключим библиотеку math для решения задачи
import math
x=float(input("введите число:"))

# Округлим до целого число x
x=math.floor(x)   #Окргуление до ближайшего нижнего числа
x=math.ceil(x)    #Округление до ближайшего верхнего числа
x = x + x
print(x)


#Задание 2
import math

# Попросим пользователя ввести координаты
x1=float(input("Введите координаты x1:"))
y1=float(input("Введите координаты y1:"))
x2=float(input("Введите координаты x2:"))
y2=float(input("Введите координаты y2:"))

# Добавим формулу для вычисления расстояния с помощью math
p=math.sqrt((x1 - x2) ** 2 + (y1 - y2) ** 2)
print(p)


#Задание 3

# Просим пользователя ввести число
x=int(input("введите четырехзначное число:"))

# Узнаем цифирю из разряда тысячных
x1=x // 1000
# Тоже самое для сотен, десятков и единиц
x2=(x // 100) % 10
x3=(x // 10) % 10
x4=x%10
print("Цифра в позиции тысяч равна", x1)
print("Цифра в позиции сотен равна", x2)
print("Цифра в позиции десятков равна", x3)
print("Цифра в позиции единиц равна", x4)


# Задание 4

import math
n=int(input("Введите количество школьников:"))
k=int(input("Введите количество мандаринов:"))

# Узнаем какое число мандаринов достанется каждому из школьников
v=math.floor(k / n)

# Какое количество останется в корзине
b=math.floor(k % n)
print("Мандаринов для каждого школьника:", v)
print("Мандаринов в корзине", b)


# Задание 5
import math
Min=int(input("Введите минуты:"))

# Переведем в часы
hour=math.floor(Min / 60)

# Узнаем какое количество осталось минут, не переведенных в часы
minute=math.floor(Min % 60)
print(Min, "мин - это", hour, "час", minute, "минут")


# Задание 6
import math
x=float(input("Введите градусы:"))

# Переведем градусы в радианы
x=math.radians(x)

# Вычислим тригонометрическую функцию
u=math.sin(x) + math.cos(x) + math.tan(x) ** 2
print(u)


# Задание 7
m=int(input("Введите Ваше место:"))

# с помощью функции if зададим условие для определения номера купе в вагоне
if m >= 1 and m <= 4:
    print("Номер купе 1")
    
# Повторим также для остальных мест в купе
if m >= 6 and m <= 8:
    print("Номер купе 2")
if m >= 9 and m <= 12:
    print("Номер купе 3")
if m >= 13 and m <= 16:
    print("Номер купе 4")
if m >= 17 and m <= 20:
    print("Номер купе 5")
if m >= 21 and m <= 24:
    print("Номер купе 6")
if m >= 25 and m <= 28:
    print("Номер купе 7")
if m >= 29 and m <= 32:
    print("Номер купе 8")
if m >= 34 and m <= 36:
    print("Номер купе 9")
